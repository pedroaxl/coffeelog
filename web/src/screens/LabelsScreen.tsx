/** Labels (badge 6b) — Phase 0 placeholder; batch QR/PNG export in Phase 3. */
export function LabelsScreen() {
  return (
    <div className="min-h-full bg-cream px-[22px] pt-4">
      <h1 className="mb-4 font-serif text-[26px] font-semibold">Labels</h1>
      <p className="text-[13px] text-muted">Label printing arrives in Phase 3.</p>
    </div>
  );
}
