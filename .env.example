# CoffeeLog server configuration

# Port the server listens on (serves both the API and the built web app)
PORT=8080

# Directory for the SQLite database and uploaded photos.
# Back up CoffeeLog by copying this whole directory.
DATA_DIR=./data
